[project]
name = "fm-dlp"
version = "2.1.0"
description = "fm-dlp — search engine and installer of videos/tracks from YouTube"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "ytmusicapi",
    "cyclopts",
    "mutagen",
    "yt-dlp",
    "tqdm"
]
